---
name: 5.3-portal-reaction-tests
task: 5.3
lane: TEST
size: M
status: draft
depends-on: 5.1
---

# Reaction + portal integration tests

> Lane: TEST. Owns `**/*.test.ts` (reactions/portal). Parallel with 5.1/5.2.

## Context
Lock reaction upsert semantics and customer access control, incl. stale-session handling. Advances
FR-15, C2–C4 and rule 4 (rule 12).

**Required reading:** [`CLAUDE.md`](../../CLAUDE.md) · [`task-breakdown.md`](../task-breakdown.md) #5.3 ·
the 5.1 plan.

## Goal & non-goals
- **Goal:** integration tests for reaction upsert/toggle/replace idempotency, customer access control,
  stale-session (deleted customer → 401/redirect).
- **Non-goals:** No UI tests; no trainer-reaction (not a feature).

## Files I own
- `test/integration/reactions.test.ts` (or co-located `*.test.ts`).

## Approach — cases
1. **Upsert**: no reaction → PUT creates; **same emoji again → removed** (toggle); **different emoji →
   replaced** (still exactly one row).
2. **Idempotency**: repeated identical PUTs converge to a stable state.
3. **Access control**: customer A cannot react on B's entry (→404); a trainer session cannot react
   (customer-only); wrong-trainer customer cookie rejected.
4. **Stale session**: customer deleted mid-session → reaction/portal request → 401/redirect (not 500).
5. **Reflected to trainer**: after a reaction, the entry DTO the trainer reads includes it.

## Rule check
- [ ] §4 customer-owns-entry + role checks (P0). §12 behavior-locking. §17 Lane — only `*.test.ts`.

## Test plan
- [ ] `npm test` green; DB reset between tests.

## Summary
- **What changed:** _(fill in)_
