---
name: 3.3-workspace-tests
task: 3.3
lane: TEST
size: M
status: draft
depends-on: 3.1
---

# Groups + Customers integration tests

> Lane: TEST. Owns `**/*.test.ts` (workspace). Parallel with 3.1/3.2; exercises the real BE.

## Context
Lock CRUD correctness and the P0 ownership/IDOR guarantees before InBody data is attached. Advances
FR-6…FR-10 and rule 4 (rule 12).

**Required reading:** [`CLAUDE.md`](../../CLAUDE.md) · [`task-breakdown.md`](../task-breakdown.md) #3.3 ·
the 3.1 plan (route behavior).

## Goal & non-goals
- **Goal:** integration tests for group/customer CRUD, ownership/IDOR, customer-requires-group,
  per-trainer name uniqueness, delete cascade (+ object cleanup invoked).
- **Non-goals:** No UI tests; no InBody (Phase 4).

## Files I own
- `test/integration/groups.test.ts`, `test/integration/customers.test.ts` (or co-located `*.test.ts`).

## Approach — cases
1. **Groups**: create + list returns only the session trainer's groups; trainer A cannot read B's.
2. **Customers create**: requires owned `groupId` (missing/unowned → 400/404); password hashed.
3. **Name uniqueness**: duplicate normalized name under the same trainer → 409; same name under a
   different trainer → OK.
4. **IDOR**: trainer A `PATCH`/`DELETE` on B's customer → 404 (no existence leak).
5. **Delete cascade**: deleting a customer removes its entries (DB) and **calls MinIO delete** for each
   objectKey (assert via a stubbed/recording s3 or a real test bucket count).
6. **Patch**: rename, move group, reset password each persist.

## Rule check
- [ ] §4 IDOR cases are the centerpiece. §11 cascade cleanup asserted.
- [ ] §12 Behavior-locking before Phase 4. §17 Lane — only `*.test.ts`.

## Test plan
- [ ] `npm test` green against the test DB + test bucket; tables/bucket reset between tests.

## Summary
- **What changed:** _(fill in)_
