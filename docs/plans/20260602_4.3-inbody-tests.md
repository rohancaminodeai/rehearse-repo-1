---
name: 4.3-inbody-tests
task: 4.3
lane: TEST
size: M
status: complete
depends-on: 4.1
---

# InBody integration + HEIC unit tests

> Lane: TEST. Owns `**/*.test.ts` (inbody). Parallel with 4.1/4.2; exercises real BE + test bucket.

## Context
Lock upload/storage correctness and the P0 file-route IDOR guarantee. Advances FR-11…FR-14 and rules
4, 8, 11 (rule 12).

**Required reading:** [`CLAUDE.md`](../../CLAUDE.md) · [`task-breakdown.md`](../task-breakdown.md) #4.3 ·
the 4.1 plan (route behavior).

## Goal & non-goals
- **Goal:** integration tests for upload (incl **HEIC→JPEG**), file-route IDOR, delete-removes-object,
  comment edit; unit test of the HEIC convert path.
- **Non-goals:** No UI tests; no reaction (Phase 5).

## Files I own
- `test/integration/inbody.test.ts`, `src/server/lib/heic.convert.test.ts` (or co-located `*.test.ts`).

## Approach — cases
1. **Upload jpeg**: object lands in test bucket; DB row has UUID key + metadata; comment optional.
2. **Upload HEIC**: stored object is **JPEG** (content-type/magic bytes), preview-able; unconvertible
   input → 400, **no orphan object**.
3. **Size/type**: oversized or disallowed type → 400.
4. **DB-failure cleanup**: simulate DB write failure → object deleted (no orphan).
5. **File-route IDOR**: customer A cannot `GET /api/inbody/[B's id]/file` (→404); trainer cannot fetch
   another trainer's entry; `?download=1` sets attachment header.
6. **Delete**: DB row gone **and** MinIO object removed. **Patch comment** persists + plain text.

## Rule check
- [ ] §4 IDOR on file route is the centerpiece (P0). §8 JPEG-after-HEIC, UUID key. §11 delete cleanup.
- [ ] §12 Behavior-locking. §17 Lane — only `*.test.ts`.

## Test plan
- [ ] `npm test` green; bucket emptied between tests; uses a small real `.heic` fixture.

## Summary
- **What changed:** Added `test/integration/inbody.test.ts` (22) + `src/server/lib/heic.convert.test.ts`
  (5). Added a real `test/fixtures/sample.heic` (+ `sample.jpg`) fixture (valid HEIC, converts to JPEG).
- **Integration coverage:** upload jpeg (201; UUID objectKey `^[0-9a-f-]{36}\.jpg$`, never raw filename;
  metadata preserved; DTO never leaks `objectKey`; comment optional); **HEIC→JPEG** (stored object is
  JPEG — magic `ff d8 ff` via the proxy; objectKey `.jpg`, contentType image/jpeg); rejections
  (text/plain → 400; `.heic`-named non-HEIC bytes → 400 + no entry; over `MAX_UPLOAD_BYTES` → 400 + no
  entry); **DB-failure cleanup** (spy `appPrisma.inbodyEntry.create` reject → no row + no orphan object,
  rule 11); **file-route IDOR (P0)** — owning trainer/owning customer → 200; other customer/other trainer
  → 404; no session → 401; `?download=1` → attachment header; list scoping (other trainer → 404, missing
  customerId → 400); PATCH comment persists verbatim incl. literal `<b>`/`&` (plain text), IDOR → 404;
  DELETE removes DB row **and** MinIO object, IDOR → 404 (row+object intact).
- **Unit coverage:** `isHeic` allow/deny matrix; `heicToJpeg(sample.heic)` → JPEG magic;
  `heicToJpeg(garbage)` → `ValidationError`. (Fixture path resolved via `process.cwd()` — `import.meta.url`
  resolves wrong under the jsdom unit project.)
- **Verified:** full gate green — **87 tests** (10 files), typecheck + lint clean. No production bugs
  found (the heic.ts ArrayBuffer bug was caught and fixed by the orchestrator before the lanes ran).
