---
name: 6.2-green-gate
task: 6.2
lane: TEST
size: S
status: complete
depends-on: 6.1
---

# Full suite green + access-control coverage

> Lane: TEST. Sequential, after polish. The final automated gate before manual verification.

## Context
Guarantee the whole suite passes and the security-critical paths are meaningfully covered (rule 13).

**Required reading:** [`CLAUDE.md`](../../CLAUDE.md) · [`task-breakdown.md`](../task-breakdown.md) #6.2 ·
[`mvp-decisions.md`](../mvp-decisions.md) §verification.

## Goal & non-goals
- **Goal:** `npm run lint && npm run typecheck && npm test` all **green**; meaningful coverage on auth,
  access-control (IDOR), and upload/delete.
- **Non-goals:** No 100%-coverage chase; no E2E/Playwright (deferred per ADR).

## Files I own
- `**/*.test.ts` (any gap-filling tests), CI-style script invocation only.

## Approach
1. Run the full gate against the test containers; fix flakies (DB/bucket reset between tests).
2. Verify coverage exists for: cross-account IDOR (groups/customers/inbody file/reaction), HEIC
   upload, delete-removes-object, reaction toggle/replace.
3. Add any missing high-value cases the per-phase suites left thin.

## Rule check
- [ ] §13 Green gate enforced. §4 IDOR coverage present. §12 tests reflect real runs (no faking).
- [ ] §17 Lane — only `*.test.ts`.

## Test plan
- [ ] `docker compose -f docker-compose.test.yml up -d` then `npm run lint && npm run typecheck && npm test` green.

## Summary
- **What changed:** Ran the full gate (green + stable across two consecutive runs) and audited the
  security-critical coverage. Added **2** tests — the one genuine gap was the symmetric role-cross: a
  valid **customer** session must not satisfy a **trainer-only** route (`POST /api/groups` and
  `POST /api/customers` → 401), now locked in `test/integration/groups.test.ts`. Everything else was
  already meaningfully covered; nothing padded (rule 2).
- **Coverage audit (all ✅ covered unless noted):** auth happy path / bad password 401 / dup email 409 /
  customer login wrong slug·name·password / cookie httpOnly (secure is intentionally prod-only, so not
  asserted under test); role separation both directions (🔧 added customer→trainer-route); IDOR across
  trainers (groups/customers/inbody) and across customers (file proxy + reaction) → 404; storage
  HEIC→JPEG + UUID key + disallowed/oversize 400 + no-orphan-on-DB-failure; delete cascade removes MinIO
  object; reaction toggle/replace/one-row + emoji allowlist.
- **Verified:** `npm run lint` clean · `npm run typecheck` clean · `npm test` **101 passed / 101**
  (11 files), stable. No production bugs found.
