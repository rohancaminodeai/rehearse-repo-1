---
name: 6.2-green-gate
task: 6.2
lane: TEST
size: S
status: draft
depends-on: 6.1
---

# Full suite green + access-control coverage

> Lane: TEST. Sequential, after polish. The final automated gate before manual verification.

## Context
Guarantee the whole suite passes and the security-critical paths are meaningfully covered (rule 13).

**Required reading:** [`CLAUDE.md`](../../CLAUDE.md) · [`task-breakdown.md`](../task-breakdown.md) #6.2 ·
[`mvp-decisions.md`](../mvp-decisions.md) §verification.

## Goal & non-goals
- **Goal:** `npm run lint && npm run typecheck && npm test` all **green**; meaningful coverage on auth,
  access-control (IDOR), and upload/delete.
- **Non-goals:** No 100%-coverage chase; no E2E/Playwright (deferred per ADR).

## Files I own
- `**/*.test.ts` (any gap-filling tests), CI-style script invocation only.

## Approach
1. Run the full gate against the test containers; fix flakies (DB/bucket reset between tests).
2. Verify coverage exists for: cross-account IDOR (groups/customers/inbody file/reaction), HEIC
   upload, delete-removes-object, reaction toggle/replace.
3. Add any missing high-value cases the per-phase suites left thin.

## Rule check
- [ ] §13 Green gate enforced. §4 IDOR coverage present. §12 tests reflect real runs (no faking).
- [ ] §17 Lane — only `*.test.ts`.

## Test plan
- [ ] `docker compose -f docker-compose.test.yml up -d` then `npm run lint && npm run typecheck && npm test` green.

## Summary
- **What changed:** _(fill in)_
