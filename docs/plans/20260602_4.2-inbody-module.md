---
name: 4.2-inbody-module
task: 4.2
lane: FE
size: M
status: complete
depends-on: 1.2, 3.2
---

# InBody module UI (upload, grid, preview, edit, delete)

> Lane: FE. Owns `src/components/inbody-module.tsx`,
> `src/app/(trainer)/dashboard/customers/[customerId]/page.tsx`.
> Parallel with 4.1 (BE) and 4.3 (TEST). Edits the customer page that 3.2 created → serial with P3, OK.

## Context
The center canvas for a selected customer: upload images, see them, preview/download, edit comment,
delete. Advances FR-11…FR-14, T5–T6 and the mockup's drop-zone + comment-below layout.

**Required reading:** [`CLAUDE.md`](../../CLAUDE.md) · [`requirements.md`](../requirements.md) §inbody ·
[`task-breakdown.md`](../task-breakdown.md) #4.2 · the mockup.

## Goal & non-goals
- **Goal:** `inbody-module.tsx` with drop-zone upload, entries grid, **preview modal** (Dialog),
  download button, edit-comment, delete; the customer page renders it; comment shown **below** the image.
- **Non-goals:** No charts/metrics, no AI report (deferred placeholders only if needed), no reaction UI
  (Phase 5 adds the badge here — serial).

## Files I own
- `src/app/(trainer)/dashboard/customers/[customerId]/page.tsx` — customer header + `<InbodyModule>`.
- `src/components/inbody-module.tsx` — upload drop-zone, grid of entry cards, preview Dialog, actions.

## Approach
1. Upload via `multipart/form-data` POST to `/api/inbody`; show pending + error states; `router.refresh()`
   after. Accept jpeg/png/heic; client-side size hint (server is the gate).
2. Entry image `src` = the **proxy route** `/api/inbody/[id]/file` (never a public URL). Preview modal
   shows the full image + comment + download (`?download=1`).
3. Edit comment = inline/Dialog text field → `PATCH /api/inbody/[id]`; plain-text render (rule 16).
4. Delete = confirm Dialog → `DELETE`; optimistic-free, just refresh.
5. **Empty state**: no entries → drop-zone prompt (rule 16).

## Rule check
- [ ] §4 Images loaded only via the auth proxy route. §16 empty/error states, plain-text comment.
- [ ] §2 No extra viz; just images + comment. §9 Strict TS. §7 Validate before upload.
- [ ] §17 Lane paths only; imports frozen types/ui.

## Test plan
- [ ] `npm run lint && npm run typecheck`.
- [ ] Manual: upload (incl `.heic` → preview not blank), download, edit comment, delete (gone + object removed).

## Summary
- **What changed:** Added the customer center canvas — `dashboard/customers/[customerId]/page.tsx`
  (client page; unwraps Next-15 `params` with `use()`, best-effort resolves the customer name from
  `GET /api/groups` since there's no single-customer endpoint, renders `<InbodyModule>`) and
  `components/inbody-module.tsx` (drag-drop + click upload with optional comment, entries grid, preview
  Dialog, edit-comment, delete-confirm Dialog).
- **Decisions:** images load **only** via the auth proxy `/api/inbody/[id]/file` (rule 4), download via
  `?download=1`; no object URLs. Upload uses `FormData` with no manual Content-Type (browser sets the
  boundary); client checks are non-blocking hints (server is the gate). HEIC entries show a
  "download to view" placeholder (browsers can't render HEIC). Comments render as plain text
  (`whitespace-pre-wrap`, no `dangerouslySetInnerHTML`, rule 16). Local React state + `refetch()` after
  each mutation (no state lib, rule 2). Reaction badge intentionally deferred to Phase 5. Kept UI copy
  **English** to match the existing pages (the rest of the app is English, not Korean).
- **States (rule 16):** loading / error+retry / empty (dashed upload prompt); pending + inline error on
  every mutation; per-action toasts via the existing sonner host.
- **Verified:** typecheck + lint clean; full suite green.
