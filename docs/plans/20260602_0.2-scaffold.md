---
name: 0.2-scaffold
task: 0.2
lane: INFRA
size: M
status: draft
depends-on: 0.0, 0.1
---

# Scaffold Next.js + shadcn + ESLint/Prettier

> Lane: INFRA. Sequential foundation (Phase 0) — runs before any fan-out. Establishes `package.json`
> and `components/ui/**`, both **frozen** after Phase 1 (rule 17).

## Context
Stand up the app skeleton every later lane builds in. No fan-out is safe until this exists.

**Required reading:** [`CLAUDE.md`](../../CLAUDE.md) · [`architecture.md`](../architecture.md) §folders ·
[`task-breakdown.md`](../task-breakdown.md) #0.2.

## Goal & non-goals
- **Goal:** Next.js App Router + **TS strict** + Tailwind; shadcn initialized with the components we use
  (Dialog, Button, Input, Table, Card, Toast, DropdownMenu); ESLint + Prettier configured.
- **Non-goals:** No features, no routes beyond a booting home page, no extra UI components.

## Files I own
- `package.json`, `tsconfig.json` (strict), `next.config.*`, `tailwind.config.*`, `postcss.config.*`,
  `.eslintrc*`, `.prettierrc*`, `src/app/layout.tsx`, `src/app/page.tsx`, `src/components/ui/**`,
  `components.json` (shadcn).

## Approach
1. **Scaffold conflict note (from the build plan):** `create-next-app` needs a near-empty dir but
   `CLAUDE.md`/`docs/` already exist. Scaffold in a temp dir and merge in, preserving the existing docs
   and `.gitignore`.
2. `tsconfig` strict: `strict: true`, `noUncheckedIndexedAccess` optional but recommended; no `any`.
3. `npx shadcn init` then add only the listed primitives. Set emerald primary in the theme.
4. Wire scripts: `dev`, `lint` (eslint), `typecheck` (`tsc --noEmit`), `build`. (`test` added in 0.3,
   `prisma.seed` field added in 0.4/1.1 — coordinate since package.json freezes.)

## Rule check
- [ ] §1 Approved stack only; no extra deps. §9 TS strict, no `any`.
- [ ] §2 No over-engineering — only the UI primitives we'll use.
- [ ] §17 This sets the frozen `package.json` + `components/ui/**`; later lanes only import them.

## Test plan
- [ ] `npm run dev` boots a page. `npm run lint` and `npm run typecheck` pass.

## Summary
- **What changed:** _(fill in)_
