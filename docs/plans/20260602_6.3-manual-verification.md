---
name: 6.3-manual-verification
task: 6.3
lane: MANUAL
size: S
status: complete
depends-on: 6.2
---

# End-to-end manual verification

> Lane: MANUAL. The final demo-readiness walkthrough against a real stack.

## Context
Confirm the full trainer + customer flows work against real Postgres + MinIO before demo. Mirrors
[`mvp-decisions.md`](../mvp-decisions.md) §verification.

**Required reading:** [`CLAUDE.md`](../../CLAUDE.md) · [`task-breakdown.md`](../task-breakdown.md) #6.3.

## Goal & non-goals
- **Goal:** a scripted, reproducible manual run covering trainer flow, customer flow, and access control.
- **Non-goals:** No automation here (that's 6.2); no load testing.

## Steps
1. `cp .env.example .env`; `docker compose up -d` → Postgres + MinIO healthy (console :9001); bucket exists.
2. `npx prisma migrate dev && npx prisma db seed`; `npm run dev`.
3. **Trainer**: signup → login → create group → add customer (set password) → open InBody module →
   upload an image (appears → preview modal → download → edit comment → delete) → confirm object
   appears/disappears in MinIO console. **Upload a real `.heic`** → preview renders (converted), not blank.
4. **Customer**: open `/portal/[slug]/login` → log in with name + password → dashboard → preview/
   download → add/change emoji reaction → reload (persists) → trainer view shows the reaction.
5. **Access control**: logged-out `/dashboard` and `/portal/[slug]/dashboard` redirect to the correct
   login; a customer **cannot** fetch another customer's `/api/inbody/[id]/file` (404).

## Rule check
- [ ] §4 IDOR manually confirmed. §8 HEIC preview works. §11 delete removes the MinIO object.
- [ ] §15 `.env.example` matched `docker-compose.yml` (compose came up clean).

## Test plan
- [ ] All steps pass; note any defect as a follow-up task (don't silently pass).

## Summary

### Automated verification (run against the real stack — all ✅)
Performed against a **production build** (`npm run build`, then `next start -p 3100`) on real dev Postgres
+ MinIO booted from `.env`/`docker-compose.yml`.
- **Stack + parity (rule 15):** `docker compose up -d` healthy; `.env` ≡ `.env.example` (byte-identical);
  bucket `inbody` present; `prisma migrate deploy` + `db seed` clean.
- **Production build:** `npm run build` ✅ — all 14 routes compiled, Edge middleware bundled (39.5 kB);
  API/proxy/reaction routes are dynamic, login/signup/dashboard static as expected.
- **Trainer:** login (seed `coach@demo.test` / `password123`) → 200 DTO (no `passwordHash` leak); authed
  `GET /api/groups` → seed data; unauthenticated → 401; logged-out `/dashboard` → 307 → `/login`.
- **Upload + storage (rules 8/11):** uploaded the real `test/fixtures/sample.heic` → 201,
  `contentType: image/jpeg` (HEIC converted), `originalFilename` preserved; UUID objectKey
  (`16b3…-….jpg`, not the raw filename); file proxy → 200 `image/jpeg` magic `ff d8`; `?download=1` →
  `Content-Disposition: attachment`.
- **Customer:** portal login (slug `demo-coach`, `Jane Doe` / `test1234`) → 200; reaction `🔥` → 200
  ReactionDTO; **trainer GET reflects the reaction**; logged-out `/portal/demo-coach/dashboard` → 307 →
  portal login.
- **Access control / IDOR (rule 4):** no-session file → 401; a **second trainer** ("attacker") cannot
  read / list / delete the victim's entry → **404** each; a trainer session cannot react (customer-only)
  → 401; the victim's customer cookie opening `/portal/attacker-coach/dashboard` → 307 → attacker login
  (slug↔trainer binding, rule 5).
- **Delete cascade (rule 11):** object PRESENT in MinIO before delete → `DELETE /api/inbody/[id]` 200 →
  object **GONE** after; DB row removed. Test data cleaned up; DB left at seed state.

### Manual browser click-through (owner to run — automation can't render pixels)
The flows below are logic-verified above; this confirms the **rendered UI** in a browser:
1. `npm run dev` → visit `/signup`, create a trainer → redirected into `/dashboard`.
2. Create a group → add a customer (set a password) → open the customer → drag-drop an image →
   it appears in the grid; open preview → image renders, comment shows below; click Download → file saves.
3. Upload a real `.heic` → preview renders the converted JPEG (NOT blank/broken).
4. Edit a comment → persists; delete an entry → confirm dialog → it disappears.
5. Copy the customer's portal link → open `/portal/[slug]/login` in a private window → log in with
   name + password → see the entries read-only → click an emoji → it highlights; reload → it persists;
   back in the trainer view the entry shows the reaction badge.
6. Empty states: a brand-new group shows the "no customers" hint; a customer with no entries shows the
   upload prompt; a customer with no results shows the portal empty message.

### Result
All automated steps passed; no defects found. The MVP is demo-ready pending the visual click-through.
